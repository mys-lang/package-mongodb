from . import Client
from bson import Document
from bson import String
from string import pretty

func main():
    client = Client()
    client.connect()
    print(pretty(str(client.list_databases())))
    client.insert("test", "inventory", [Document([("x", String("y"))])])
    print(pretty(str(client.find("test", "inventory"))))
